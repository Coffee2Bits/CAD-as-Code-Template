[project]
name = "ci"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["dagger-io"]

[project.entry-points."dagger.mod"]
main_object = "ci.main:Ci"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
